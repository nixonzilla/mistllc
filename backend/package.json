{
	"name": "mistllc-backend",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build": "npm run build",
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"@types/express": "^5.0.3",
		"@types/node": "^24.3.0",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.32.0"
	}
}
