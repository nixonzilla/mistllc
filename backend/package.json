{
  "name": "mistllc-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm ci && npm run bundle",
    "bundle": "esbuild src/index.ts --bundle --outdir=dist",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@cloudflare/types": "^7.0.0",
    "cors": "^2.8.5",
    "hono": "4.9.7"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "typescript": "^5.6.2",
    "wrangler": "4.32.0"
  }
}
