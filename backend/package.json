{
  "name": "mistllc-backend-worker",
  "version": "1.0.0",
  "description": "MISTLLC backend Worker with KV support",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc",
    "deploy": "npm run build && npx tsx scripts/upload-kv.ts && wrangler deploy",
    "deploy:fast": "npm run build && wrangler deploy" 
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.2025.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "tsx": "^4.19.2"
  }
}
